<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>نتائج التحاليل الطبية</title>
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
  <style>
    :root {
      --primary-color: #3498db;
      --secondary-color: #2c3e50;
      --accent-color: #e74c3c;
      --light-color: #1a1a2e;
      --dark-color: #f0f0f0;
      --success-color: #2ecc71;
      --border-radius: 8px;
      --box-shadow: 0 4px 6px rgba(0, 0, 0, 0.3);
      --transition: all 0.3s ease;
    }
    
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }
    
    body {
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      background: linear-gradient(135deg, #0f0c29 0%, #302b63 50%, #24243e 100%);
      color: var(--dark-color);
      min-height: 100vh;
      line-height: 1.6;
      padding: 20px;
    }
    
    .container {
      max-width: 1200px;
      margin: 0 auto;
      background-color: #16213e;
      border-radius: var(--border-radius);
      box-shadow: var(--box-shadow);
      overflow: hidden;
      border: 1px solid rgba(255, 255, 255, 0.1);
    }
    
    header {
      background: linear-gradient(to right, #0f3460, #16213e);
      color: white;
      padding: 25px 20px;
      text-align: center;
      border-bottom: 1px solid rgba(255, 255, 255, 0.1);
      position: relative;
    }
    
    header h1 {
      font-size: 1.8rem;
      margin-bottom: 10px;
      color: #e6e6e6;
    }
    
    .back-btn {
      position: absolute;
      top: 20px;
      right: 20px;
      background-color: rgba(255, 255, 255, 0.1);
      color: white;
      border: 1px solid rgba(255, 255, 255, 0.2);
      padding: 10px 15px;
      border-radius: var(--border-radius);
      cursor: pointer;
      transition: var(--transition);
      text-decoration: none;
      display: inline-block;
    }
    
    .back-btn:hover {
      background-color: rgba(255, 255, 255, 0.2);
      transform: translateY(-2px);
    }
    
    main {
      padding: 30px 20px;
      background-color: #1a1a2e;
      min-height: 400px;
    }
    
    .results-info {
      text-align: center;
      margin-bottom: 30px;
    }
    
    .results-info p {
      font-size: 1.1rem;
      color: rgba(255, 255, 255, 0.8);
    }
    
    .phone-number {
      font-weight: bold;
      color: var(--primary-color);
      direction: ltr;
      display: inline-block;
      background-color: rgba(52, 152, 219, 0.1);
      padding: 5px 10px;
      border-radius: 4px;
    }
    
    .table-responsive {
      overflow-x: auto;
      margin-bottom: 30px;
    }
    
    table {
      width: 100%;
      border-collapse: collapse;
      box-shadow: 0 2px 3px rgba(0,0,0,0.2);
      background-color: #0f3460;
      border-radius: var(--border-radius);
      overflow: hidden;
    }
    
    th, td {
      padding: 15px;
      text-align: right;
      border-bottom: 1px solid rgba(255, 255, 255, 0.1);
      color: #e6e6e6;
    }
    
    th {
      background-color: #16213e;
      font-weight: bold;
      color: var(--primary-color);
      position: sticky;
      top: 0;
    }
    
    tr:last-child td {
      border-bottom: none;
    }
    
    tr:hover {
      background-color: rgba(255, 255, 255, 0.05);
    }
    
    .action-buttons {
      display: flex;
      gap: 10px;
    }
    
    .btn {
      display: inline-flex;
      align-items: center;
      justify-content: center;
      padding: 8px 15px;
      border-radius: var(--border-radius);
      font-weight: bold;
      text-decoration: none;
      transition: var(--transition);
      font-size: 0.9rem;
      border: none;
      cursor: pointer;
    }
    
    .btn-download {
      background-color: var(--primary-color);
      color: white;
    }
    
    .btn-download:hover {
      background-color: #2980b9;
      color: white;
      transform: translateY(-2px);
      box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2);
    }
    
    .btn-view {
      background-color: var(--success-color);
      color: white;
    }
    
    .btn-view:hover {
      background-color: #27ae60;
      color: white;
      transform: translateY(-2px);
      box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2);
    }
    
    .btn i {
      margin-left: 5px;
    }
    
    .no-results {
      text-align: center;
      padding: 60px 20px;
      background-color: #0f3460;
      border-radius: var(--border-radius);
      margin-top: 20px;
      box-shadow: var(--box-shadow);
      border: 1px solid rgba(255, 255, 255, 0.1);
    }
    
    .no-results-icon {
      font-size: 4rem;
      color: rgba(255, 255, 255, 0.2);
      margin-bottom: 20px;
    }
    
    .no-results h3 {
      color: #e6e6e6;
      margin-bottom: 15px;
    }
    
    .no-results p {
      color: rgba(255, 255, 255, 0.7);
      margin-bottom: 25px;
    }
    
    .search-again {
      display: inline-block;
      padding: 12px 25px;
      background: linear-gradient(to right, var(--primary-color), #0f3460);
      color: white;
      text-decoration: none;
      border-radius: var(--border-radius);
      font-weight: bold;
      transition: var(--transition);
      border: none;
      cursor: pointer;
    }
    
    .search-again:hover {
      background: linear-gradient(to right, #2980b9, #133c6e);
      color: white;
      transform: translateY(-2px);
      box-shadow: 0 5px 10px rgba(0, 0, 0, 0.2);
    }
    
    footer {
      text-align: center;
      margin-top: 40px;
      color: rgba(255, 255, 255, 0.6);
      font-size: 0.9rem;
      padding: 20px;
      background-color: rgba(0, 0, 0, 0.2);
    }
    
    @media (max-width: 768px) {
      th, td {
        padding: 10px;
      }
      
      .btn {
        padding: 6px 10px;
        font-size: 0.8rem;
      }
      
      header h1 {
        font-size: 1.5rem;
      }
      
      .back-btn {
        position: relative;
        top: auto;
        right: auto;
        margin-bottom: 15px;
        display: inline-block;
      }
    }
    
    @media (max-width: 576px) {
      .action-buttons {
        flex-direction: column;
        gap: 5px;
      }
      
      .btn {
        width: 100%;
        justify-content: center;
      }
    }
  </style>
</head>
<body>
  <div class="container">
    <header>
      <a href="/" class="back-btn">
        <i class="fas fa-arrow-right"></i> العودة
      </a>
      <h1>نتائج التحاليل الطبية</h1>
    </header>

    <main>
      <div class="results-info">
        <p>النتائج المتاحة للرقم: <span class="phone-number"><%= phoneNumber || "غير محدد" %></span></p>
      </div>
      
      <% if (Array.isArray(result) && result.length > 0) { %>
        <div class="table-responsive">
          <table>
            <thead>
              <tr>
                <th>اسم المريض</th>
                <th>رقم الهاتف</th>
                <th>الإجراءات</th>
              </tr>
            </thead>
            <tbody>
              <% result.forEach(r => { %>
                <tr>
                  <td><%= r.name %></td>
                  <td><%= r.phone %></td>
                  <td>
                    <div class="action-buttons">
                      <a href="/download/<%= r.file %>" class="btn btn-download">
                        <i class="fas fa-download"></i> تحميل
                      </a>
                      <a href="/view/<%= r.file %>" target="_blank" class="btn btn-view">
                        <i class="fas fa-eye"></i> عرض
                      </a>
                    </div>
                  </td>
                </tr>
              <% }) %>
            </tbody>
          </table>
        </div>
      <% } else { %>
        <div class="no-results">
          <div class="no-results-icon">☹️</div>
          <h3>لا توجد نتائج متاحة</h3>
          <p>لا توجد نتائج متاحة للرقم المطلوب</p>
          <p>يرجى التأكد من صحة الرقم أو مراجعة المركز الطبي</p>
          <a href="/" class="search-again">البحث مرة أخرى</a>
        </div>
      <% } %>
    </main>
    
    <footer>
      <p>© 2025 معمل التحاليل الطبية. جميع الحقوق محفوظة.</p>
    </footer>
  </div>
</body>
</html>
