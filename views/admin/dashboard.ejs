<!DOCTYPE html>
<html lang="ar">
<head>
  <meta charset="UTF-8">
  <link rel="stylesheet" href="/styles.css">
  <title>لوحة تحكم المسؤول</title>
</head>
<body>
  <h2>لوحة التحكم - رفع نتيجة جديدة</h2>
  <form method="POST" action="/admin/upload" enctype="multipart/form-data">
    <input type="text" name="name" placeholder="اسم المريض" required>
    <input type="text" name="phone" placeholder="رقم الهاتف" required>
    <input type="email" name="email" placeholder="البريد الإلكتروني (لإشعار Gmail)" required>
    <input type="file" name="pdf" accept="application/pdf" required>
    <button type="submit">رفع النتيجة</button>
  </form>

  <h3>النتائج الحالية</h3>
  <ul>
    <% results.forEach(result => { %>
      <li>
        <strong><%= result.name %></strong> - <%= result.phone %> -
        <a href="/download/<%= result.file %>">تحميل</a>
      </li>
    <% }) %>
  </ul>
</body>
</html>
