<!DOCTYPE html>
<html lang="ar">
<head>
  <meta charset="UTF-8">
  <title>لوحة التحكم</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      direction: rtl;
      margin: 30px;
      background-color: #f9f9f9;
    }
    h1, h2 {
      color: #333;
    }
    form {
      margin-bottom: 20px;
      background: #fff;
      padding: 15px;
      border: 1px solid #ccc;
    }
    input, button {
      margin: 5px;
      padding: 8px;
    }
    table {
      width: 100%;
      border-collapse: collapse;
      background: #fff;
    }
    th, td {
      border: 1px solid #ccc;
      padding: 10px;
      text-align: center;
    }
    th {
      background-color: #eee;
    }
    .btn {
      padding: 6px 12px;
      margin: 2px;
      text-decoration: none;
      border: none;
      cursor: pointer;
    }
    .delete { background-color: #e74c3c; color: white; }
    .notify { background-color: #3498db; color: white; }
    .download { background-color: #2ecc71; color: white; }
    .view { background-color: #f39c12; color: white; }
  </style>
</head>
<body>

  <h1>لوحة التحكم</h1>

  <h2>رفع نتيجة جديدة</h2>
  <form action="/admin/upload" method="POST" enctype="multipart/form-data">
    <input type="text" name="name" placeholder="الاسم" required />
    <input type="text" name="phone" placeholder="رقم الهاتف" required />
    <input type="email" name="email" placeholder="البريد الإلكتروني" required />
    <input type="file" name="pdf" accept="application/pdf" required />
    <button type="submit" class="btn">رفع التحليل</button>
  </form>

  <h2>جميع النتائج</h2>
  <table>
    <thead>
      <tr>
        <th>الاسم</th>
        <th>الهاتف</th>
        <th>الإيميل</th>
        <th>الملف</th>
        <th>التاريخ</th>
        <th>خيارات</th>
      </tr>
    </thead>
    <tbody>
      <% results.forEach(result => { %>
        <tr>
          <td><%= result.name %></td>
          <td><%= result.phone %></td>
          <td><%= result.email %></td>
          <td><%= result.file %></td>
          <td><%= new Date(result.date).toLocaleString('ar-EG') %></td>
          <td>
            <a href="/download/<%= result.file %>" class="btn download">تحميل</a>
            <a href="/view/<%= result.file %>" target="_blank" class="btn view">عرض</a>
            <form action="/admin/delete" method="POST" style="display:inline;">
              <input type="hidden" name="phone" value="<%= result.phone %>" />
              <button type="submit" class="btn delete">🗑 حذف</button>
            </form>
            <form action="/admin/notify" method="POST" style="display:inline;">
              <input type="hidden" name="phone" value="<%= result.phone %>" />
              <button type="submit" class="btn notify">📧 إشعار</button>
            </form>
          </td>
        </tr>
      <% }) %>
    </tbody>
  </table>

</body>
</html>
