<!DOCTYPE html>
<html>
<head><title>لوحة التحكم</title></head>
<body>
  <h1>لوحة التحكم</h1>
  <table border="1">
    <tr><th>الاسم</th><th>الهاتف</th><th>الإيميل</th><th>الملف</th><th>خيارات</th></tr>
    <% results.forEach(r => { %>
      <tr>
        <td><%= r.name %></td>
        <td><%= r.phone %></td>
        <td><%= r.email %></td>
        <td><a href="/download/<%= r.file %>">تحميل</a> | <a href="/view/<%= r.file %>" target="_blank">عرض</a></td>
        <td>
          <form method="POST" action="/admin/delete" style="display:inline;">
            <input type="hidden" name="phone" value="<%= r.phone %>"/>
            <button type="submit">🗑️ حذف</button>
          </form>
          <form method="POST" action="/admin/notify" style="display:inline;">
            <input type="hidden" name="phone" value="<%= r.phone %>"/>
            <button type="submit">📧 إرسال إشعار</button>
          </form>
        </td>
      </tr>
    <% }) %>
  </table>
</body>
</html>
